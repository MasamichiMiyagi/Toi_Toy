<%# Ajaxの為にpost_comment]の後に続く「, local: false」を削除している %>
<%= form_with(model:[game, post_comment]) do |f| %>

  <%= render 'layouts/errors', obj: f.object %>

  <%= f.text_area :comment, rows: '5', class: "w-100" %>
  <%= f.submit "送信", class: "btn btn-sm btn-success mb-3" %>
<% end %>