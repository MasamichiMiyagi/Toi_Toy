<table class='table'>
  <tr><%= image_tag customer.get_profile_image(100,100) %></tr>
  <tr>
    <th>name</th>
    <th><%= customer.name %></th>
  </tr>
</table>

<div class='row'>
  <%= link_to edit_customer_path(current_customer), class: "btn btn-light btn-outline-secondary btn-block edit_customer_#{customer.id}" do %>
    <i class="fas fa-user-cog"></i>
  <% end %>
</div>