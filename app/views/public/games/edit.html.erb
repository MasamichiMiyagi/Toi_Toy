<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h1>Editing Tweet</h1>
      <%= render 'layouts/errors', obj: @game %>
      <%= render 'form', game: @game %>
      <%= link_to 'Show', @game, class: "game_#{@game.id}" %> |
      <%= link_to 'Back', games_path, class: "back" %>
    </div>
  </div>
</div>